<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.msb.mall.ware.dao.WareSkuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.msb.mall.ware.entity.WareSkuEntity" id="wareSkuMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="wareId" column="ware_id"/>
        <result property="stock" column="stock"/>
        <result property="skuName" column="sku_name"/>
        <result property="stockLocked" column="stock_locked"/>
    </resultMap>
    <insert id="addStock" >
        UPDATE wms_ware_sku SET stock=stock+#{skuNum} WHERE sku_id=#{skuId} AND ware_id=#{wareId}
    </insert>

    <update id="lockSkuStock">
        UPDATE `wms_ware_sku` SET stock_locked = stock_locked + #{count} WHERE sku_id=#{skuId} AND ware_id = #{wareId}
    </update>

    <select id="getSkuStock" resultType="java.lang.Long">
        SELECT SUM(stock-stock_locked) FROM wms_ware_sku WHERE sku_id=#{skuId}
    </select>

    <select id="listHashStock" resultMap="wareSkuMap">
        SELECT * FROM `wms_ware_sku` WHERE sku_id = #{skuId} AND stock - stock_locked > 0
    </select>


    <!-- ====================================== -->
    <!-- 新增：查询sku的锁定库存记录（stock_locked > 0） -->
    <!-- ====================================== -->
    <select id="listLockedStock" resultMap="wareSkuMap">
        SELECT id, sku_id, ware_id, stock, sku_name, stock_locked
        FROM wms_ware_sku
        WHERE sku_id = #{skuId} AND stock_locked > 0
    </select>

    <!-- ====================================== -->
    <!-- 新增：扣减实际库存 + 释放锁定库存（原子操作） -->
    <!-- ====================================== -->
    <update id="deductStockAndReleaseLock">
        UPDATE wms_ware_sku
        SET
        stock = stock - #{currentDeductNum},        <!-- 扣减实际库存 -->
        stock_locked = stock_locked - #{currentDeductNum} <!-- 释放锁定库存 -->
        WHERE
        sku_id = #{skuId}
        AND ware_id = #{wareId}
        AND stock >= #{currentDeductNum}          <!-- 防止实际库存不足 -->
        AND stock_locked >= #{currentDeductNum}    <!-- 防止锁定库存不足 -->
    </update>



</mapper>